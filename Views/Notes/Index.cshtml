@model IEnumerable<Sample_MVC.Models.Notes>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NotesTitle)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NotesDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NotesCreatedAt)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NotesTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NotesDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NotesCreatedAt)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NotesId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NotesId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NotesId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
